public class UpdateUser {

    private static final String UPDATE_EMAIL =
            "UPDATE users " +
                    "SET email = ? " +
                    "WHERE id = ?";

    public static void main(String[] args) {
        
    }
}
